# Misc

Basic digital building blocks in Verilog.

| Module | Description |
|--------|-------------|
| `frequency_divider.v` | Clock frequency divider |
| `latchrs.v` | RS latch |
| `dffrs.v` | D flip-flop with reset/set |

Each module has a corresponding `_tb.v` testbench.
